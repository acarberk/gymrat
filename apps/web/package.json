{
  "name": "@gymrat/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Next.js 16 PWA frontend (scaffolding pending phase 3)",
  "scripts": {
    "lint": "echo 'lint placeholder — wired up in phase 3'",
    "type-check": "echo 'type-check placeholder — wired up in phase 3'",
    "build": "echo 'build placeholder — wired up in phase 3'",
    "test": "echo 'test placeholder — wired up in phase 3'",
    "dev": "echo 'dev placeholder — wired up in phase 3'",
    "clean": "rm -rf .next .turbo"
  },
  "devDependencies": {
    "@gymrat/config": "workspace:*"
  }
}
