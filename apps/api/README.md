# @gymrat/api

NestJS backend API.

Scaffolding lands in Phase 2 — Prisma, Postgres, Redis, Auth (JWT + Passport),
Mailer, Throttler, Turnstile, User module.

See repo root `CLAUDE.md` for the full phase plan.
